{
  "version": 2,
  "installCommand": "cd ../.. && npm install && npm run build --workspace=@my-billing/database",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
